ROUND 4 — ThriveAbility Care. Three fixes Jack flagged from the Round 3 preview:
1. Header still looks cut off — fix the visual containment, not just padding
2. Hero doesn't fill the viewport — gap shows below before next section
3. Home page is missing an enquiry form — add one above the final CTA band

Read CLAUDE.md, progress.md, decisions.md.

Working directory: ~/Desktop/thriveability-care

Execute in order. STOP at each marker.

=== STEP 1: HEADER VISUAL CONTAINMENT FIX ===

1a. The actual problem from the Round 3 screenshot: the header background colour --cream (#F7FAFC) is barely distinguishable from the page background and the surrounding browser chrome at the top. Even with 28px padding, the header has no visible top edge — it bleeds upward and feels chopped off because there is no contrast.

1b. Two changes work together to fix this:

    CHANGE A — Make the header transparent over the hero.
    Heroes are dark teal-overlaid images. A transparent header sitting over them lets the hero own the full viewport from edge to edge, AND the white wordmark + nav links read clearly against the dark hero overlay. This is the premium pattern (NSP, Bosland, every modern site).
    
    CHANGE B — When scrolled past the hero, header gets a solid background with shadow.
    Already partially in place via .site-header.scrolled. We just lean into it.

1c. Updated .site-header rule:
    .site-header {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 1000;
      background: transparent;
      padding: 28px 0;
      transition: var(--transition);
      border-bottom: 1px solid transparent;
    }
    .site-header.scrolled {
      background: var(--cream);
      box-shadow: var(--shadow-sm);
      border-bottom: 1px solid rgba(15, 61, 76, 0.08);
      padding: 18px 0;
    }
    
    The .scrolled state has tighter padding (18px vs 28px) so the header compacts when sticky — feels more responsive and less obstructive.

1d. With transparent header on hero, the logo wordmark and nav links need to be readable on the dark teal hero overlay. Add scoped overrides:
    
    .site-header:not(.scrolled) .header-logo {
      color: var(--cream);
    }
    .site-header:not(.scrolled) .logo-tagline {
      color: rgba(247, 250, 252, 0.7);
    }
    .site-header:not(.scrolled) .nav-links a {
      color: var(--cream);
    }
    .site-header:not(.scrolled) .nav-links a::after {
      background: var(--terracotta);  /* sky stays the active accent */
    }
    .site-header:not(.scrolled) .nav-toggle span {
      background: var(--cream);
    }
    /* nav-cta button keeps sky bg + cream text — already readable on teal */

1e. The hero now needs to start at the absolute top of the page (under the transparent header) instead of being pushed down 100px by main padding. Update main:
    main { padding-top: 0; }
    
    BUT non-hero pages (none of ours, but service detail pages and inner pages all have hero too — confirm). Every page in this site starts with a hero, so padding-top: 0 on main is fine for all 10 pages. Confirm by checking each page's first section is a hero.

1f. Show me the updated .site-header rules, the scoped overrides, and the new main padding.

>>> STOP 1: Confirm header containment plan.

=== STEP 2: HERO FILLS THE VIEWPORT ===

2a. Current .hero rule has min-height: 90vh. The Round 3 screenshot shows visible white below the hero before the next section — the hero isn't filling the screen.

2b. Update home hero to min-height: 100dvh (dynamic viewport height — handles mobile browser chrome correctly, falls back gracefully on desktop).
    
    Update .hero (home page default):
    .hero {
      position: relative;
      min-height: 100dvh;
      min-height: 100vh; /* fallback for browsers without dvh */
      display: flex;
      align-items: center;
      color: var(--cream);
      overflow: hidden;
    }

2c. Inner page hero (.hero-inner) should stay shorter. Update:
    .hero.hero-inner {
      min-height: 70vh;
    }
    
    (Round 1 set this to 60vh, but with the transparent header pattern, inner page heroes need slightly more height to feel intentional. 70vh is the right balance.)

2d. The contact page hero was 50vh per Round 1. Keep contact at .hero-inner default (70vh) — the 50vh override was pre-transparent-header and now feels too short.

2e. Confirm the .hero-content max-width 760px stays — copy still constrained, just sitting in a taller hero.

2f. Show me the updated .hero rules and which pages use which hero variant.

>>> STOP 2: Confirm hero sizing.

=== STEP 3: ENQUIRY FORM ON HOME PAGE ===

3a. Add a NEW section to index.html positioned BETWEEN the "Why Choose ThriveAbility" split section and the "Final CTA" forest band.

3b. Section structure — two-column split layout with form on the right, contextual copy on the left. This keeps the home page narrative intact (hero → trust signals → services → why us → enquire → final CTA) and gives a strong conversion point before the visitor leaves the page.

3c. Section markup to insert in index.html:

    <section class="section section-cream-dark" data-aos="fade-up">
      <div class="container">
        <div class="enquiry-block">
          <div class="enquiry-block-text">
            <span class="eyebrow">Make an enquiry</span>
            <h2>Have a question? Start here.</h2>
            <p class="lead">Tell us about your situation or what you're looking for. Whether you're an NDIS participant, a family member or a support coordinator, our team will get back to you within 24 hours.</p>
            <div class="enquiry-block-meta">
              <div class="enquiry-meta-row">
                <span class="enquiry-meta-label">Phone</span>
                <a href="tel:1300500821" class="enquiry-meta-value">1300 500 821</a>
              </div>
              <div class="enquiry-meta-row">
                <span class="enquiry-meta-label">Email</span>
                <a href="mailto:info@thriveabilitycare.com.au" class="enquiry-meta-value">info@thriveabilitycare.com.au</a>
              </div>
              <div class="enquiry-meta-row">
                <span class="enquiry-meta-label">Hours</span>
                <span class="enquiry-meta-value">Monday to Friday, 8am to 6pm</span>
              </div>
            </div>
          </div>
          <div class="enquiry-block-form">
            <form data-formspree action="https://formspree.io/f/FORMSPREE_ID_HERE" method="POST" class="home-enquiry-form">
              <input type="hidden" name="_subject" value="ThriveAbility Care Home Page Enquiry">
              <input type="hidden" name="_format" value="plain">
              <input type="text" name="_gotcha" style="display:none">
              <div class="form-row">
                <div class="form-group">
                  <label for="home-firstName">First name</label>
                  <input id="home-firstName" name="firstName" type="text" required>
                </div>
                <div class="form-group">
                  <label for="home-lastName">Last name</label>
                  <input id="home-lastName" name="lastName" type="text" required>
                </div>
              </div>
              <div class="form-row">
                <div class="form-group">
                  <label for="home-email">Email</label>
                  <input id="home-email" name="email" type="email" required>
                </div>
                <div class="form-group">
                  <label for="home-phone">Phone</label>
                  <input id="home-phone" name="phone" type="tel">
                </div>
              </div>
              <div class="form-group">
                <label for="home-message">How can we help?</label>
                <textarea id="home-message" name="message" rows="4" required></textarea>
              </div>
              <button type="submit" class="btn btn-primary form-submit">Send Enquiry</button>
              <p class="form-disclaimer">We respect your privacy. Your details are only used to respond to your enquiry.</p>
            </form>
            <div class="form-success">Thanks for getting in touch — we'll be in contact within 24 hours.</div>
          </div>
        </div>
      </div>
    </section>

3d. Add corresponding CSS to assets/css/styles.css (place near the .map-block rule from Round 3 — they're sibling components):

    .enquiry-block {
      display: grid;
      grid-template-columns: 1fr 1.1fr;
      gap: 64px;
      align-items: start;
    }
    .enquiry-block-text h2 {
      margin-top: 8px;
      margin-bottom: 20px;
    }
    .enquiry-block-text .lead {
      max-width: 460px;
      margin-bottom: 40px;
    }
    .enquiry-block-meta {
      display: flex;
      flex-direction: column;
      gap: 20px;
      padding-top: 28px;
      border-top: 1px solid rgba(15, 61, 76, 0.12);
    }
    .enquiry-meta-row {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }
    .enquiry-meta-label {
      font-size: 0.8125rem;
      text-transform: uppercase;
      letter-spacing: 0.12em;
      color: var(--charcoal-light);
      font-weight: 500;
    }
    .enquiry-meta-value {
      font-size: 1.0625rem;
      color: var(--charcoal);
      font-weight: 500;
    }
    a.enquiry-meta-value:hover {
      color: var(--terracotta);
    }
    .enquiry-block-form {
      background: var(--white);
      padding: 40px;
      border-radius: var(--radius);
      box-shadow: var(--shadow-md);
    }
    .home-enquiry-form .form-row + .form-row,
    .home-enquiry-form .form-row + .form-group,
    .home-enquiry-form .form-group + .form-group {
      margin-top: 0;
    }
    .form-disclaimer {
      margin-top: 16px;
      font-size: 0.8125rem;
      color: var(--charcoal-light);
      line-height: 1.5;
    }
    
    @media (max-width: 900px) {
      .enquiry-block {
        grid-template-columns: 1fr;
        gap: 40px;
      }
      .enquiry-block-form {
        padding: 28px;
      }
    }

3e. The form action uses FORMSPREE_ID_HERE placeholder — same as the contact page form. This is intentional. Round 4 is not the round to wire backend. Do not fail any audit on this.

3f. Verify the existing JS form submission handler in assets/js/main.js handles forms with [data-formspree] generically — the home form uses the same attribute, so it should work the same way once Formspree is wired. Confirm by reading main.js. If the handler queries forms with querySelectorAll instead of querySelector, no JS change needed. If it only handles one form, update to handle all forms with [data-formspree].

3g. Show me the new index.html section, the new CSS block, and confirm JS handler covers both forms.

>>> STOP 3: Confirm enquiry form is in.

=== STEP 4: FOOTER SEPARATION FROM CTA BAND ===

4a. Looking at the Round 3 screenshot of the bottom of the home page: the final CTA band (.section-forest, deep teal) sits flush against the footer (.site-footer, deepest teal #0A2D38). The visual separation is weak — both are dark teal.

4b. Add a subtle horizontal divider effect between the CTA band and footer. Either:
    - Add 1px top border to .site-footer in a slightly lighter teal: border-top: 1px solid rgba(247, 250, 252, 0.08);
    OR
    - Reduce the .section-forest bottom padding so the CTA breathes less and feels distinct, then let the footer have its own start
    
    Use the border approach. Cleaner.

4c. Update .site-footer rule to add: border-top: 1px solid rgba(247, 250, 252, 0.08);

4d. Show me the change.

>>> STOP 4: Confirm footer separation.

=== STEP 5: VISUAL SANITY PASS ===

5a. Walk through the rendered output mentally for the four big surfaces:

    HOME PAGE TOP:
    - Transparent header sits over hero, white wordmark + nav links visible against dark teal overlay
    - Hero fills 100dvh — no white gap below
    - "Contact Us" sky-blue button in nav top-right pops against transparent
    - Page scrolls, header transitions to solid cream with shadow + tighter padding

    INNER PAGE TOP (e.g. about, a service detail):
    - Same transparent → solid header pattern
    - Hero is 70vh — fills first screen but lets next section peek

    HOME PAGE — ENQUIRY SECTION:
    - Cream-dark bg, two-column grid: left contextual copy with phone/email/hours rows, right white form card with shadow
    - Form fields stack cleanly on mobile

    HOME → FOOTER TRANSITION:
    - Final teal CTA band, then 1px subtle border, then deepest-teal footer

5b. Flag anything you'd recommend adjusting before commit. Don't change — just report.

>>> STOP 5: Confirm visual coherence.

=== STEP 6: ANTI-VIBECODE AUDIT (focused) ===

6a. Re-run relevant checks on the changed files:
    - No emoji in new markup
    - No new inline styles except: hero bg URLs (already there), the _gotcha honeypot input style="display:none" (acceptable accessibility hide pattern)
    - All new <input> have <label for>
    - All new <a> have hrefs
    - No lorem ipsum

6b. Internal links + asset paths still resolve. Round 4 changes touch:
    - assets/css/styles.css
    - assets/js/main.js (only if handler needed updating to handle multiple forms)
    - index.html (header overrides + new enquiry section)
    No other pages touched in this round — but the header CSS changes affect ALL pages because header is shared. Visually verify a service detail page still renders the header correctly (transparent over its hero, solid on scroll).

6c. Show me the audit summary.

>>> STOP 6: Confirm clean audit.

=== STEP 7: COMMIT + REPORT ===

7a. Run:
    git add -A
    git status

7b. Show staged file list.

7c. Commit:
    git commit -m "Round 4: transparent header over hero, hero fills 100dvh, home page enquiry form, footer separation from CTA band"

7d. Push:
    git push

7e. Update decisions.md with a Round 4 entry:
    - Header pattern: transparent over hero, solid cream on scroll. Logo + nav scoped to cream colour when not scrolled. Reason: Round 3 padding fix didn't address the underlying issue — the cream header had no contrast against viewport edge. Going transparent makes the hero own the full screen and matches the premium pattern used by NSP/Bosland.
    - Hero height: 100dvh on home, 70vh on inner pages. Reason: Jack flagged white gap below hero on Round 3 preview. 90vh wasn't filling.
    - Home enquiry form: added between "Why Choose" and final CTA band. Two-column layout, form right side. Form action uses FORMSPREE_ID_HERE placeholder pending backend round.
    - Footer top border for separation from final CTA band.

7f. Update progress.md adding "Round 4 [x] header pattern + hero sizing + home enquiry form" line.

7g. Commit and push docs:
    git add decisions.md progress.md
    git commit -m "Round 4 docs"
    git push

7h. Final report:
    1. Files modified (count + list)
    2. Header pattern: before / after summary
    3. Hero min-height: before / after for home + inner pages
    4. Home enquiry form: confirm rendered, confirm Formspree placeholder used (not real ID)
    5. JS handler change (yes/no — if querySelectorAll already used, no change)
    6. Footer top border: confirm
    7. Visual concerns or follow-up needed
    8. Local preview command reminder
    9. Git status

DO NOT START A NEW ROUND. STOP HERE.